block('controller')
    .elem('product')
    .mod('action', 'show')
    .def()(function() {
    return applyCtx({
        block: 'product-card',
        mods: {type: 'show'},
        data: this.ctx.data
    });
});
